using System; //referencia para o namespace chamado System, namespace padrão que contém funções básicas de manipulação de programas.

namespace PrimeiroProjeto //declaração de namespace desse arquivo. (não obrigatório, questão de organização)
{
    class Program //declaração da classe, unidade básica da POO.
    {
        static void Main(string[] args) //membro da classe - declaração padrão da linguagem para identificar o Entry Point da aplicação
        { //tudo colocado aqui será executado primeiro no programa
            Console.WriteLine("Olá Mundo!"); 
        }
    }
}
